/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ContactIconsListComponent } from './contact-icons-list.component';

describe('Component: ContactIconsList', () => {
  it('should create an instance', () => {
    let component = new ContactIconsListComponent();
    expect(component).toBeTruthy();
  });
});
